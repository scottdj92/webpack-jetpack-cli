#!/usr/bin/env node

'use strict';

const program = require('commander');

program
    .version('')
    .command('')
    .description('')
    .option('')
    .option('')
    .action('');
program.parse(process.argv);
